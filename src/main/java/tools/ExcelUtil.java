package tools;

import log4j.LoggerControler;

import java.io.FileOutputStream;
import java.io.IOException;
public class ExcelUtil {
  /*  LoggerControler log=LoggerControler.getLogger(ExcelUtil.class);
    public static void createExcel(String filePath){
        if (filePath.endsWith(".xls")){
            workbook=new HSSFWorkbook();
            Sheet sheet1=workbook.creatSheet("sheet1");
            FileOutputStream out=null;
            try{
                out=new FileOutputStream(filePath);
                workbook.write(out);

            }catch(Exception e){
                e.printStackTrace();
            }finally{
                try{
                    out.close();

                }catch(IOException e){
                    e.printStackTrace();
                }
            }

        }else{
           log. severe("文件格式必须为xls格式");
        }
    }
    */
}
